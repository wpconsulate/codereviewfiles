export const _editPhone = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditPhone', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}

export const _editPetName = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditPetName', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}

export const _editPatientName = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditPatientName', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}

export const _editRxItem = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditRxItem', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}

export const _editInstructions = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditInstructions', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}

export const _editQty = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditQty', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}
export const _editRefill = ( rx_id, tab, val, nav ) => {
    nav.navigate('EditRefill', {
        rxID: rx_id,
        val: val,
        type: tab
    });
}
